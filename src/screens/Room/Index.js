import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Room = () => {
  return (
    <View>
      <Text>Room</Text>
    </View>
  );
};

export default Room;

const styles = StyleSheet.create({});
